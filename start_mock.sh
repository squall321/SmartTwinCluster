#!/bin/bash
################################################################################
# HPC 웹 서비스 Mock Mode 시작 스크립트 (프로젝트 루트용)
################################################################################

cd "$(dirname "$0")"

if [ -f "dashboard/start_mock.sh" ]; then
    echo "🎭 HPC 웹 서비스 Mock Mode 시작 중..."
    ./dashboard/start_mock.sh "$@"
else
    echo "❌ 오류: dashboard/start_mock.sh 파일을 찾을 수 없습니다."
    echo "   현재 디렉토리: $(pwd)"
    exit 1
fi
