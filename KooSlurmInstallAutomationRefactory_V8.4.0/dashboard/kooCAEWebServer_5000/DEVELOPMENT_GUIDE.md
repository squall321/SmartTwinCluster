# 🛠️ KooCAE Development Guide

## 🚀 **처음 시작하는 경우**

프로젝트를 처음 받았다면 **자동 초기화**를 먼저 실행하세요:

```bash
# Windows
setup.bat

# Linux/macOS
./setup.sh
```

**🐍 Python 버전 선택**: 초기화 과정에서 Python 3.8+ 중 원하는 버전을 선택할 수 있습니다 (기본: 3.13 권장)

**🔍 Python 버전만 확인**: 설치된 Python 버전들을 먼저 확인하려면:
```bash
# Windows
check_python_versions.bat

# Linux/macOS
./check_python_versions.sh
```

자세한 초기 설정은 **[SETUP_GUIDE.md](SETUP_GUIDE.md)** 를 참조하세요.

---

## 📋 **개발 모드 안내**

이 프로젝트는 **두 가지 주요 개발 모드**로 구성되어 있습니다:

### 🚀 **1. Flask 서버 모드** (웹 서비스)
Python Flask 기반 웹 서버 개발 및 디버깅

### 🔬 **2. C++ 테스트 모드** (핵심 라이브러리)
C++ 핵심 라이브러리 개발 및 디버깅

---

## 🚀 **Flask 서버 모드 사용법**

### **실행 방법**

#### VS Code에서 실행 (권장)
1. **F5** 키 누르기
2. **"🚀 Flask Server (Debug)"** 선택
3. 자동으로 디버깅 모드에서 Flask 서버 시작

#### 터미널에서 실행
```bash
# 가상환경 활성화 (이미 활성화된 경우 생략)
venv\Scripts\activate

# Flask 서버 실행
python app.py
```

#### Command Palette에서 실행
1. **Ctrl+Shift+P**
2. **"Tasks: Run Task"** 선택
3. **"🚀 Start Flask Server"** 선택

### **주요 기능**
- ✅ **파일 변환**: K 파일 → STL/GLB 변환
- ✅ **3D 시각화**: 웹 브라우저에서 3D 모델 확인
- ✅ **사용자 관리**: SQLite 기반 사용자 시스템
- ✅ **API 서비스**: RESTful API 제공

### **접속 주소**
- 로컬: http://localhost:5000
- 네트워크: http://0.0.0.0:5000

---

## 🔬 **C++ 테스트 모드 사용법**

### **빌드 방법**

#### 자동 빌드 (권장)
```bash
# 전체 환경 설정 + 빌드
.\setup_and_build.bat

# 또는 빠른 빌드
.\quick_build.bat
```

#### VS Code에서 빌드
1. **Ctrl+Shift+P**
2. **"Tasks: Run Task"**
3. **"🔧 Configure C++ Project"** (최초 1회)
4. **"Build C++ Test Runner"** (일반 빌드)

### **디버깅 방법**

#### VS Code에서 디버깅 (권장)
1. **F5** 키 누르기
2. 다음 중 선택:
   - **"🔬 C++ Test Runner (Debug)"** - 기본 test.k 파일 사용
   - **"🔬 C++ Test Runner (Custom K-file)"** - 사용자 K 파일 지정
   - **"🔬 C++ Test Runner (Release)"** - Release 버전

#### 직접 실행
```bash
# Debug 버전 실행
.\build\cpp_only\Debug\test_runner.exe build\cpp_test\test.k

# Release 버전 실행  
.\build\cpp_only\Release\test_runner.exe build\cpp_test\test.k

# 사용자 K 파일로 실행
.\build\cpp_only\Debug\test_runner.exe "C:\path\to\your\file.k"
```

### **테스트 파일 위치**
- **기본 테스트 파일**: `build/cpp_test/test.k`
- **추가 테스트 파일**: 원하는 위치에 `.k` 파일 배치

---

## 🎯 **개발 워크플로우**

### **🔄 Python 확장 모듈 개발 시**
1. C++ 코드 수정 (`build/` 폴더 내)
2. `.\setup_and_build.bat` 실행
3. Flask 서버에서 테스트

### **🔄 C++ 핵심 라이브러리 개발 시**
1. C++ 코드 수정
2. **F5** → **"🔬 C++ Test Runner (Debug)"**
3. 브레이크포인트로 디버깅

### **🔄 Flask 웹 서비스 개발 시**
1. Python 코드 수정 (`app.py`, `routes/`, `services/`)
2. **F5** → **"🚀 Flask Server (Debug)"**
3. 브라우저에서 테스트

---

## 🛠️ **유용한 VS Code 명령어**

### **Tasks (Ctrl+Shift+P → "Tasks: Run Task")**
- **🚀 Start Flask Server** - Flask 서버 시작
- **🐍 Build Python Extension** - Python 확장 모듈 빌드
- **🔧 Configure C++ Project** - C++ 프로젝트 구성
- **🧹 Clean All Builds** - 모든 빌드 파일 정리
- **📋 Show Build Info** - 현재 빌드 상태 확인

### **디버깅 구성 (F5)**
- **🚀 Flask Server (Debug)** - Flask 디버깅
- **🐍 Python: Current File** - 현재 Python 파일 실행
- **🔬 C++ Test Runner (Debug)** - C++ 디버깅
- **🔬 C++ Test Runner (Custom K-file)** - 사용자 파일로 C++ 디버깅

---

## 📁 **프로젝트 구조**

```
kooCAEWebServer/
├── 🚀 Flask 서버 모드
│   ├── app.py                 # 메인 Flask 앱
│   ├── routes/                # API 라우트
│   ├── models/                # 데이터베이스 모델
│   ├── services/              # 비즈니스 로직
│   └── static/                # 정적 파일
│
├── 🔬 C++ 테스트 모드  
│   ├── build/
│   │   ├── cpp_test/          # C++ 테스트 파일
│   │   ├── cpp_only/          # 빌드 결과
│   │   ├── general/           # 공통 유틸리티
│   │   ├── nodeManager/       # 노드 관리
│   │   ├── elementManager/    # 요소 관리
│   │   └── ...               # 기타 매니저들
│   │
├── 🔧 빌드 스크립트
│   ├── setup_and_build.bat   # 전체 빌드
│   ├── quick_build.bat       # 빠른 빌드
│   └── compilewin.bat        # 기존 빌드
│
└── 📄 설정 파일
    ├── .vscode/              # VS Code 설정
    ├── requirements.txt      # Python 의존성
    └── config.py            # Flask 설정
```

---

## 🚨 **문제 해결**

### **Flask 서버가 시작되지 않을 때**
```bash
# 의존성 재설치
pip install -r requirements.txt

# Python 확장 모듈 재빌드
.\setup_and_build.bat
```

### **C++ 빌드가 실패할 때**
```bash
# 모든 빌드 파일 정리
# VS Code: Ctrl+Shift+P → "🧹 Clean All Builds"

# CMake 재구성
# VS Code: Ctrl+Shift+P → "🔧 Configure C++ Project"

# 전체 다시 빌드
.\setup_and_build.bat
```

### **디버깅이 작동하지 않을 때**
1. Visual Studio 2022 설치 확인
2. Python 가상환경 활성화 확인
3. **F5** → 올바른 디버깅 구성 선택

---

## 📞 **추가 도움**

문제가 발생하면:
1. **📋 Show Build Info** 태스크로 상태 확인
2. Terminal에서 오류 메시지 확인
3. `.vscode/` 설정이 올바른지 확인

**행복한 개발 되세요! 🎉**
