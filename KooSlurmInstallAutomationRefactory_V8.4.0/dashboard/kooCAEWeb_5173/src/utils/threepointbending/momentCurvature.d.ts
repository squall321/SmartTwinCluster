import { MaterialParams, DeformationModel } from "../../types/threepointbending";
export declare function momentCurvature(curvature: number, height: number, width: number, material: MaterialParams, deformation: DeformationModel, integrationSteps: number): number;
