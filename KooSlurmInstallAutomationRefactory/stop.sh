#!/bin/bash
################################################################################
# HPC 웹 서비스 중지 스크립트 (프로젝트 루트용)
################################################################################

cd "$(dirname "$0")"

if [ -f "dashboard/stop_complete.sh" ]; then
    echo "🛑 HPC 웹 서비스 중지 중..."
    ./dashboard/stop_complete.sh "$@"
else
    echo "❌ 오류: dashboard/stop_complete.sh 파일을 찾을 수 없습니다."
    echo "   현재 디렉토리: $(pwd)"
    exit 1
fi
