import { SupportCondition } from "../../types/threepointbending";
export declare function computeCurvatureDistribution(curvature: number, L: number, numPoints: number, loadType: "Point" | "UDL", loadPosition: number, supportCondition: SupportCondition): number[];
