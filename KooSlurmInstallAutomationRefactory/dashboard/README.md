# HPC 클러스터 인증 포털 & 대시보드 시스템

## 📋 프로젝트 개요

**프로젝트명**: HPC Cluster Authentication Portal & Dashboard
**목적**: SSO 인증 기반 HPC 클러스터 통합 관리 시스템
**버전**: v4.0.0 (Phase 0-2 완료)
**개발 환경**: Mock Mode / Production Mode 지원
**인증**: SAML 2.0 SSO + JWT Token

---

## 🎯 프로젝트 단계 (Phases)

### ✅ Phase 0: Infrastructure Setup (완료)
- Redis 6.0+ (세션 저장소)
- Nginx (HTTPS 리버스 프록시)
- SAML IdP (개발/테스트용)
- Apptainer Sandbox (GPU VNC)

### ✅ Phase 1: Auth Portal (완료)
- Auth Portal Frontend (포트 4431) - SSO 로그인 UI
- Auth Portal Backend (포트 4430) - JWT 발급/검증
- 그룹 기반 권한 시스템 (4개 그룹)
- Redis 세션 관리 (8시간 유효)

### ✅ Phase 2: JWT Integration (완료)
- Dashboard Backend JWT 인증 적용
- 권한 기반 API 접근 제어
- JWT 미들웨어 (`@jwt_required`, `@permission_required`)

### 🔄 Phase 3: Frontend Integration (진행 예정)
- Dashboard Frontend JWT 통합
- 토큰 자동 갱신
- 만료 시 재로그인 처리

---

## 🏗️ 시스템 아키텍처 (Phase 0-2)

```
                          ┌─────────────────────┐
                          │   사용자 브라우저    │
                          └──────────┬──────────┘
                                     │
                     ┌───────────────┴───────────────┐
                     │                               │
                     v                               v
         ┌────────────────────────┐      ┌────────────────────────┐
         │   Auth Portal (4431)   │      │    Dashboard (3010)    │
         │   - SSO 로그인         │      │    - 작업 관리         │
         │   - 서비스 메뉴        │──JWT─►│    - 클러스터 모니터링 │
         └───────────┬────────────┘      └───────────┬────────────┘
                     │                               │
                     v                               v
         ┌────────────────────────┐      ┌────────────────────────┐
         │  Auth Backend (4430)   │      │  Dashboard API (5010)  │
         │  - JWT 발급/검증       │      │  - JWT 인증 ✅         │
         │  - SAML 처리           │      │  - Slurm API           │
         │  - 권한 관리           │      │  - Storage API         │
         └───────────┬────────────┘      └───────────┬────────────┘
                     │                               │
                     v                               │
         ┌────────────────────────┐                  │
         │    Redis (6379)        │                  │
         │    - JWT 세션 저장     │                  │
         │    - 8시간 TTL         │                  │
         └────────────────────────┘                  │
                                                     │
         ┌────────────────────────┐                  │
         │   SAML IdP (7000)      │                  │
         │   - 테스트 IdP         │                  │
         └────────────────────────┘                  │
                                                     │
         ┌────────────────────────────────────────────┤
         │                                            │
         v                                            v
┌─────────────────────┐                    ┌──────────────────┐
│  Nginx (443)        │                    │ Slurm Cluster    │
│  - HTTPS 프록시     │                    │ - Jobs           │
│  - 4430, 4431, 3010 │                    │ - Nodes          │
│  - SSL/TLS          │                    │ - Partitions     │
└─────────────────────┘                    └──────────────────┘
```

### 인증 흐름

```
[1] 사용자 → Auth Portal (4431) "Login with SSO"
     │
[2] Auth Portal → SAML IdP (7000) 리다이렉트
     │
[3] IdP → 사용자 인증 (username/password)
     │
[4] IdP → Auth Backend (4430) SAML Response
     │
[5] Auth Backend → JWT 토큰 생성 + Redis 저장
     │
[6] Auth Portal → 서비스 메뉴 페이지 (사용자 그룹별 서비스 표시)
     │
[7] 사용자 → Dashboard 선택 (JWT 토큰 전달)
     │
[8] Dashboard (3010) → localStorage에 토큰 저장
     │
[9] Dashboard → Dashboard API (5010) 호출 (Bearer Token)
     │
[10] Dashboard API → JWT 검증 → 권한 확인 → 응답
```

---

## 🔧 서버 구성 요소

### 1. **Frontend Server (Port 3010)** 
[상세 문서: FRONTEND_FEATURES.md](./FRONTEND_FEATURES.md)

**기술 스택**: React 18, TypeScript, Vite, Tailwind CSS, Zustand, Three.js, Recharts

**주요 기능**:
- ✅ 메인 대시보드 (클러스터 개요, 실시간 모니터링)
- ✅ 커스텀 대시보드 (20+ 위젯, 드래그 앤 드롭, 레이아웃 저장/로드)
- ✅ 작업 관리 (제출, 취소, 상세 정보, 템플릿)
- ✅ Job 템플릿 (사전 정의, 파라미터화, 빠른 제출)
- ✅ 데이터 관리 (파일 브라우저, 업로드/다운로드, 검색, 미리보기)
- ✅ Prometheus 메트릭 (실시간 차트, 커스텀 쿼리, 다중 시계열)
- ✅ 알림 센터 (실시간 알림, 필터링, 읽음 처리)
- ✅ 리포트 생성 (PDF/Excel/CSV, 커스터마이징, 히스토리)
- ✅ 3D 시각화 (Three.js 기반 노드 배치)
- ✅ 전역 검색 (작업, 노드, 파일, 사용자)
- ✅ 다크모드 지원

**핵심 컴포넌트**:
- `Dashboard.tsx`: 메인 대시보드
- `CustomDashboard/`: 커스텀 대시보드 및 위젯
- `JobManagement/`: 작업 관리 UI
- `JobTemplates/`: Job 템플릿 관리
- `DataManagement/`: 파일 시스템 관리
- `PrometheusMetrics/`: Prometheus 차트
- `NotificationCenter/`: 알림 센터
- `Reports/`: 리포트 생성 및 히스토리

---

### 2. **Backend Server (Port 5010)**
[상세 문서: BACKEND_FEATURES.md](./BACKEND_FEATURES.md)

**기술 스택**: Flask, Flask-CORS, SQLite3, Python 3.12+

**주요 기능**:
- ✅ Slurm 통합 (sinfo, squeue, sacct, scontrol, sbatch, scancel)
- ✅ Alerts API (디스크 사용량 모니터링, 임계값 설정)
- ✅ Notifications API (알림 생성, 조회, 읽음 처리, 삭제)
- ✅ Prometheus API (PromQL 쿼리, GPU 메트릭, 다중 시계열)
- ✅ Reports API (리포트 생성, 다운로드, 히스토리)
- ✅ Templates API (Job 템플릿 관리, 파라미터화)
- ✅ Dashboard API (커스텀 대시보드 설정 저장/로드)
- ✅ Storage Management (파일 시스템 탐색, 검색, 업로드/다운로드)
- ✅ Mock/Production Mode 지원

**API Blueprint 구조**:
```python
app.py (Main)
├── alerts_api.py          # 디스크 알림 (/api/alerts/*)
├── notifications_api.py   # 알림 센터 (/api/notifications/*)
├── prometheus_api.py      # Prometheus 메트릭 (/api/prometheus/*)
├── reports_api.py         # 리포트 (/api/reports/*)
├── templates_api.py       # Job 템플릿 (/api/templates/*)
├── dashboard_api.py       # 대시보드 설정 (/api/dashboard/*)
├── directory_api.py       # 디렉토리 탐색 (/api/directory/*)
├── search_api.py          # 파일 검색 (/api/search/*)
├── preview_api.py         # 파일 미리보기 (/api/preview/*)
└── upload_api.py          # 업로드/다운로드 (/api/upload/*, /api/download/*)
```

**데이터베이스 스키마**:
- `notifications`: 알림 데이터
- `job_templates`: Job 템플릿
- `dashboard_configs`: 대시보드 설정
- `reports`: 리포트 메타데이터

---

### 3. **WebSocket Server (Port 5011)**
[상세 문서: WEBSOCKET_FEATURES.md](./WEBSOCKET_FEATURES.md)

**기술 스택**: Python websockets, asyncio

**주요 기능**:
- ✅ 채널 기반 구독 시스템 (jobs, nodes, metrics, notifications, alerts)
- ✅ 작업 상태 실시간 모니터링 (PENDING → RUNNING → COMPLETED/FAILED)
- ✅ 노드 상태 실시간 업데이트
- ✅ 시스템 메트릭 브로드캐스트 (15초 간격)
- ✅ 알림 실시간 푸시
- ✅ HTTP API 통합 (POST /broadcast, GET /health)

**채널 시스템**:
| 채널 | 업데이트 주기 | 데이터 타입 |
|------|--------------|------------|
| `jobs` | 실시간 | 작업 상태 변화 |
| `nodes` | 5초 | 노드 상태 변화 |
| `metrics` | 15초 | CPU, 메모리, GPU 메트릭 |
| `notifications` | 실시간 | 알림 메시지 |
| `alerts` | 실시간 | 긴급 알림 |

**메시지 타입**:
- `job_update`: 작업 상태 변화
- `node_update`: 노드 상태 변화
- `metrics_update`: 시스템 메트릭
- `notification`: 알림 메시지
- `alert`: 긴급 알림

---

### 4. **Prometheus (Port 9090)**
[상세 문서: PROMETHEUS_FEATURES.md](./PROMETHEUS_FEATURES.md)

**기술 스택**: Prometheus, PromQL, TSDB

**주요 기능**:
- ✅ 시계열 데이터 수집 및 저장 (15초 스크랩 간격)
- ✅ PromQL 쿼리 엔진
- ✅ 알림 규칙 평가
- ✅ 15일 데이터 보관
- ✅ Web UI (그래프, 알림, 타겟, 설정)

**스크랩 타겟**:
- `prometheus`: Prometheus 자체 모니터링
- `node_exporter`: 시스템 메트릭 (localhost:9100, node[001-004]:9100)
- `nvidia_exporter`: GPU 메트릭 (localhost:9445, node[001-002]:9445)
- `slurm_exporter`: Slurm 작업 메트릭 (localhost:9341)

**알림 규칙**:
- HighCPUUsage (> 90% for 5m)
- HighMemoryUsage (> 90% for 5m)
- LowDiskSpace (> 85% for 10m)
- HighGPUTemperature (> 80°C for 5m)
- NodeDown (exporter down for 1m)

**API 엔드포인트**:
- `/api/v1/query`: Instant query
- `/api/v1/query_range`: Range query
- `/api/v1/labels`: 레이블 목록
- `/api/v1/series`: 시계열 조회
- `/api/v1/targets`: 타겟 목록
- `/api/v1/alerts`: 활성 알림

---

### 5. **Node Exporter (Port 9100)**
[상세 문서: NODE_EXPORTER_FEATURES.md](./NODE_EXPORTER_FEATURES.md)

**기술 스택**: Prometheus Node Exporter

**주요 기능**:
- ✅ 시스템 하드웨어 및 OS 메트릭 수집
- ✅ /proc, /sys 파일시스템 모니터링
- ✅ CPU, 메모리, 디스크, 네트워크 메트릭
- ✅ 온도, 프로세스, 시스템 시간 메트릭

**주요 메트릭 카테고리**:

#### CPU
- `node_cpu_seconds_total`: CPU 시간 (idle, user, system, iowait 등)
- `node_load1/5/15`: 시스템 부하 평균

#### 메모리
- `node_memory_MemTotal_bytes`: 전체 메모리
- `node_memory_MemAvailable_bytes`: 사용 가능 메모리
- `node_memory_SwapTotal/Free_bytes`: 스왑 메모리

#### 디스크
- `node_filesystem_size/avail_bytes`: 파일시스템 크기/사용 가능
- `node_disk_read/written_bytes_total`: 디스크 I/O
- `node_disk_io_time_seconds_total`: I/O 시간

#### 네트워크
- `node_network_receive/transmit_bytes_total`: 네트워크 트래픽
- `node_network_receive/transmit_packets_total`: 패킷 수
- `node_network_receive/transmit_errs_total`: 오류 수

#### 시스템
- `node_boot_time_seconds`: 부팅 시간
- `node_procs_running/blocked`: 프로세스 수
- `node_hwmon_temp_celsius`: 하드웨어 온도

---

## 📊 데이터 흐름

### 1. 실시간 모니터링 데이터 흐름
```
Node Exporter (9100)
    ↓ (15초 간격 스크랩)
Prometheus (9090)
    ↓ (TSDB 저장)
Backend (5010) - PromQL 쿼리
    ↓ (REST API)
Frontend (3010) - 차트 시각화
```

### 2. 작업 상태 업데이트 흐름
```
Slurm Cluster
    ↓ (squeue 명령)
WebSocket (5011) - 상태 변화 감지
    ↓ (브로드캐스트)
Frontend (3010) - 실시간 업데이트
```

### 3. 알림 생성 및 전달 흐름
```
Backend (5010) - 알림 생성
    ↓ (DB 저장)
    ↓ (WebSocket 전송)
WebSocket (5011) - 브로드캐스트
    ↓ (실시간 푸시)
Frontend (3010) - 알림 표시
```

### 4. 파일 업로드/다운로드 흐름
```
Frontend (3010) - 파일 선택
    ↓ (Multipart Upload)
Backend (5010) - 파일 저장
    ↓ (/data 또는 /scratch)
Storage System
```

---

## 🚀 시작 및 중지

### Phase 0 검증 (최초 1회)
```bash
cd /home/koopark/claude/KooSlurmInstallAutomationRefactory/dashboard

# 인프라 검증
./validate_phase0.sh

# 결과:
# [PASS] Redis is running (포트 6379)
# [PASS] Nginx is running
# [PASS] Apptainer sandbox ready
# ...
```

### Phase 1 시작 (Auth Portal)
```bash
# Auth Portal 시작 (IdP + Backend + Frontend)
./start_phase1.sh

# 상태 확인
curl http://localhost:4430/health  # Auth Backend
curl http://localhost:4431         # Auth Frontend
```

**실행되는 서비스**:
1. SAML IdP (7000) - 테스트용 Identity Provider
2. Auth Backend (4430) - JWT 발급/검증
3. Auth Frontend (4431) - SSO 로그인 UI

### Dashboard 시작 (기존)
```bash
cd dashboard_refactory

# Dashboard 서비스 시작
./start_all.sh
```

**실행되는 서버**:
1. Backend (5010) - JWT 인증 적용 ✅
2. WebSocket (5011)
3. Frontend (3010)
4. Prometheus (9090)
5. Node Exporter (9100)

### 전체 시스템 중지
```bash
# Phase 1 중지
./stop_phase1.sh

# Dashboard 중지
cd dashboard_refactory
./stop_all.sh
```

### 개별 서버 제어
```bash
# Backend
cd backend_5010
./start.sh  # 시작
./stop.sh   # 중지

# Frontend
cd frontend_3010
./start.sh
./stop.sh

# WebSocket
cd websocket_5011
./start.sh
./stop.sh

# Prometheus
cd prometheus_9090
./start.sh
./stop.sh

# Node Exporter
cd node_exporter_9100
./start.sh
./stop.sh
```

### Mock Mode 전환
```bash
# Mock Mode로 시작 (개발/테스트)
./start_all_mock.sh

# Production Mode로 시작 (실제 Slurm 사용)
./start_all.sh

# 모드 토글
./toggle_mock_mode.sh
```

---

## 🔑 핵심 기능 하이라이트

### 1. **Mock/Production Mode**
- **개발 환경**: Mock Mode로 Slurm 없이 테스트
- **운영 환경**: Production Mode로 실제 Slurm 클러스터 관리
- **간편한 전환**: 환경변수 `MOCK_MODE=true/false`로 제어

### 2. **실시간 모니터링**
- **WebSocket 기반**: 작업, 노드 상태 즉시 업데이트
- **Prometheus 통합**: 시스템 메트릭 15초마다 수집
- **다양한 차트**: Line, Area, Bar, Gauge 차트 지원

### 3. **커스텀 대시보드**
- **20+ 위젯**: 클러스터, 작업, 리소스, 성능 위젯
- **드래그 앤 드롭**: React Grid Layout 기반 자유로운 배치
- **저장/로드**: 개인별 대시보드 설정 저장

### 4. **Job 템플릿**
- **사전 정의 템플릿**: Deep Learning, Bioinformatics 등
- **파라미터화**: 동적 변수 지원
- **빠른 제출**: 템플릿 선택 후 즉시 작업 제출

### 5. **알림 시스템**
- **실시간 푸시**: WebSocket을 통한 즉각 알림
- **다양한 타입**: job_completed, job_failed, alert, system
- **필터링**: 읽음/읽지 않음, 타입별 필터

### 6. **리포트 생성**
- **다양한 포맷**: PDF, Excel, CSV
- **커스터마이징**: 날짜 범위, 메트릭 선택
- **한글 지원**: NanumGothic 폰트

### 7. **데이터 관리**
- **파일 브라우저**: 디렉토리 트리 탐색
- **업로드/다운로드**: 드래그 앤 드롭, 진행률 표시
- **검색**: 이름, 확장자, 크기로 검색
- **미리보기**: 텍스트, 이미지, CSV 파일

### 8. **Prometheus 통합**
- **PromQL 지원**: 복잡한 쿼리 실행
- **GPU 메트릭**: NVIDIA GPU 모니터링
- **알림 규칙**: 임계값 기반 자동 알림

---

## 📈 성능 및 최적화

### Backend 최적화
- **캐싱**: Storage 통계 5분 캐싱
- **비동기 처리**: asyncio로 병렬 노드 조회
- **요청 제한**: Rate limiting으로 과부하 방지

### Frontend 최적화
- **Code Splitting**: 라우트별 번들 분리
- **Lazy Loading**: 컴포넌트 지연 로딩
- **Memoization**: React.memo로 불필요한 렌더링 방지
- **Debouncing**: 검색, 필터 입력 디바운스

### Prometheus 최적화
- **Recording Rules**: 자주 사용하는 쿼리 미리 계산
- **쿼리 최적화**: 레이블 필터링, 적절한 범위 사용
- **데이터 보관**: 15일 보관으로 디스크 사용량 관리

### WebSocket 최적화
- **채널 시스템**: 필요한 데이터만 구독
- **메시지 압축**: gzip 압축 (예정)
- **재연결 로직**: 자동 재연결

---

## 🧪 테스트

### Backend 테스트
```bash
cd backend_5010
python -m pytest tests/
```

### Frontend 테스트
```bash
cd frontend_3010
npm test
```

### 통합 테스트
```bash
# Mock Mode에서 전체 시스템 테스트
./start_all_mock.sh
curl http://localhost:5010/api/nodes
curl http://localhost:3010
```

---

## 🔒 보안

### API 보안
- **CORS 설정**: 허용된 도메인만 접근
- **입력 검증**: 모든 입력 데이터 검증
- **SQL Injection 방지**: Parameterized query 사용
- **파일 업로드 검증**: 파일 타입, 크기 제한

### WebSocket 보안
- **인증**: JWT 토큰 기반 인증 (예정)
- **Rate Limiting**: 메시지 전송 속도 제한
- **Origin 검증**: 허용된 origin만 연결

### Prometheus 보안
- **네트워크 제한**: localhost 또는 특정 IP만 접근
- **TLS 설정**: HTTPS 지원 (옵션)
- **방화벽**: 포트 접근 제한

---

## 📊 모니터링 대시보드 예제

### 1. 클러스터 개요 대시보드
```
┌─────────────────────────────────────────────┐
│ Cluster Overview                             │
├─────────────────────────────────────────────┤
│ Nodes: 4/4 UP    Jobs: 12 Running           │
│ CPU: 65%         Memory: 72%    GPU: 45%    │
├─────────────────────────────────────────────┤
│ [CPU Usage Chart - Last 1h]                 │
│ [Memory Usage Chart - Last 1h]              │
│ [GPU Usage Chart - Last 1h]                 │
└─────────────────────────────────────────────┘
```

### 2. Job 모니터링 대시보드
```
┌─────────────────────────────────────────────┐
│ Job Management                               │
├─────────────────────────────────────────────┤
│ Filter: [All] [Running] [Pending] [Failed]  │
├──────┬──────────┬─────────┬────────┬────────┤
│ ID   │ Name     │ Status  │ User   │ Time   │
├──────┼──────────┼─────────┼────────┼────────┤
│ 12345│ train_ml │ RUNNING │ koopark│ 2h 30m │
│ 12346│ process  │ PENDING │ user2  │ -      │
└──────┴──────────┴─────────┴────────┴────────┘
```

### 3. 리소스 사용률 대시보드
```
┌─────────────────────────────────────────────┐
│ Resource Utilization                         │
├─────────────────────────────────────────────┤
│ Node001: CPU [████████░░] 80%               │
│          MEM [██████████] 95%               │
│          GPU [██████░░░░] 60%               │
│                                              │
│ Node002: CPU [███████░░░] 70%               │
│          MEM [████████░░] 75%               │
│          GPU [█████████░] 85%               │
└─────────────────────────────────────────────┘
```

---

## 🚀 추가 기능 로드맵

### Phase 1: 완료 (v3.4.0)
- ✅ 기본 대시보드 및 모니터링
- ✅ Job 관리 및 템플릿
- ✅ 데이터 관리 (업로드/다운로드)
- ✅ Prometheus 통합
- ✅ 알림 시스템
- ✅ 리포트 생성
- ✅ 커스텀 대시보드

### Phase 2: 개선 예정 (v4.0.0)
- 🔄 **사용자 인증**: JWT 기반 로그인
- 🔄 **권한 관리**: Role-based access control
- 🔄 **Multi-tenancy**: 사용자별 격리
- 🔄 **API Rate Limiting**: 강화된 요청 제한
- 🔄 **WebSocket 압축**: gzip 압축
- 🔄 **리포트 스케줄링**: Cron 기반 자동 생성
- 🔄 **Alertmanager 통합**: 알림 라우팅 및 그룹화

### Phase 3: 신규 기능 (v5.0.0)
- 📋 **Machine Learning 예측**: 작업 완료 시간 예측
- 📋 **리소스 최적화**: 자동 리소스 할당 추천
- 📋 **Anomaly Detection**: 이상 행동 탐지
- 📋 **Cost Analysis**: 리소스 비용 분석
- 📋 **Multi-Cluster**: 여러 클러스터 통합 관리
- 📋 **Container 통합**: Apptainer/Singularity 지원
- 📋 **Workflow Management**: DAG 기반 워크플로우
- 📋 **Data Pipeline**: ETL 파이프라인 관리

---

## 🐛 문제 해결

### 1. 서버가 시작되지 않음
```bash
# 포트 사용 확인
netstat -tlnp | grep -E '3010|5010|5011|9090|9100'

# 프로세스 확인
ps aux | grep -E 'node|python|prometheus|node_exporter'

# 로그 확인
tail -f backend_5010/logs/backend.log
tail -f frontend_3010/logs/frontend.log
tail -f websocket_5011/websocket.log
```

### 2. Mock Mode가 적용되지 않음
```bash
# 환경변수 확인
cat backend_5010/start.sh | grep MOCK_MODE

# 강제 Mock Mode
export MOCK_MODE=true
./start_all.sh
```

### 3. Prometheus 메트릭이 수집되지 않음
```bash
# Node Exporter 확인
curl http://localhost:9100/metrics

# Prometheus 타겟 확인
curl http://localhost:9090/api/v1/targets | jq

# Prometheus 로그 확인
tail -f prometheus_9090/logs/prometheus.log
```

### 4. WebSocket 연결 실패
```bash
# WebSocket 서버 확인
curl http://localhost:5012/health

# 포트 확인
netstat -tlnp | grep 5011

# 로그 확인
tail -f websocket_5011/websocket.log
```

---

## 📚 참고 문서

### 🔐 인증 시스템 문서 (Phase 0-2)
- **[사용자 가이드](./USER_GUIDE.md)** - 전체 시스템 사용법
- **[빠른 참조 가이드](./QUICK_REFERENCE.md)** - 관리자용 치트시트
- [Phase 0: 인프라 설치](./setup_phase0_all.sh)
- [Phase 1: Auth Portal](./PHASE1_README.md) - SSO 인증 시스템
- [Phase 2: JWT 통합](./PHASE2_README.md) - Dashboard API 인증
- [배포 가이드](./DEPLOYMENT.md) - 새 서버 배포 방법
- **[고가용성 구성 준비](./HIGH_AVAILABILITY_PREPARATION.md)** - 사중화 및 HA 구성 가이드

### 📊 Dashboard 시스템 문서
- [Backend Server 기능 상세](./BACKEND_FEATURES.md)
- [Frontend Server 기능 상세](./FRONTEND_FEATURES.md)
- [WebSocket Server 기능 상세](./WEBSOCKET_FEATURES.md)
- [Prometheus 기능 상세](./PROMETHEUS_FEATURES.md)
- [Node Exporter 기능 상세](./NODE_EXPORTER_FEATURES.md)
- **[App Framework (5174) 셋업 가이드](./APP_5174_SETUP_GUIDE.md)** - Nginx 통합 및 자동 빌드 설정

### 외부 참고 자료
- [Slurm 공식 문서](https://slurm.schedmd.com/documentation.html)
- [React 공식 문서](https://react.dev/)
- [Flask 공식 문서](https://flask.palletsprojects.com/)
- [Prometheus 공식 문서](https://prometheus.io/docs/)
- [WebSocket 프로토콜 RFC](https://tools.ietf.org/html/rfc6455)

---

## 👥 기여자

**개발**: Koo Park  
**프로젝트 기간**: 2025.01 - 2025.10  
**버전**: v3.4.0  

---

## 📄 라이센스

이 프로젝트는 내부 사용을 목적으로 개발되었습니다.

---

## 📞 지원

문제가 발생하거나 기능 요청이 있는 경우:
1. 해당 서버의 상세 문서 확인
2. 로그 파일 확인
3. GitHub Issue 등록 (있는 경우)

---

**마지막 업데이트**: 2025-10-10  
**문서 버전**: 1.0
