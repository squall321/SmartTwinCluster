# 프로젝트 루트 실행 개선 요약

## 🎯 개선 내용

**문제점**: 웹 서비스 관련 스크립트가 `web_services/scripts/` 안에 있어 경로가 길고 불편했습니다.

**해결책**: 프로젝트 루트에 래퍼 스크립트를 생성하여 **모든 명령을 루트에서 실행** 가능하게 개선했습니다.

---

## 📁 생성된 래퍼 스크립트

### 1. start.sh
```bash
./start.sh
```
- **기능**: 전체 웹 서비스 시작
- **내부 호출**: `dashboard/start_complete.sh`

### 2. stop.sh
```bash
./stop.sh
```
- **기능**: 전체 웹 서비스 중지
- **내부 호출**: `dashboard/stop_complete.sh`

### 3. setup_web_services.sh (NEW!)
```bash
./setup_web_services.sh development --auto-start
```
- **기능**: 웹 서비스 ONE-COMMAND 설치
- **내부 호출**: `web_services/scripts/setup_web_services.sh`
- **옵션**: development/production, --auto-start, --skip-system-deps 등

### 4. generate_env_files.sh (NEW!)
```bash
./generate_env_files.sh development
```
- **기능**: 환경 변수 파일 생성 (11개 .env 파일)
- **내부 호출**: `web_services/scripts/generate_env_files.py`

### 5. health_check.sh (NEW!)
```bash
./health_check.sh
```
- **기능**: 웹 서비스 헬스 체크 (11개 서비스)
- **내부 호출**: `web_services/scripts/health_check.sh`

---

## 🔄 Before vs After

### Before (불편함)
```bash
# 경로가 길고 기억하기 어려움
python3 web_services/scripts/generate_env_files.py development
./web_services/scripts/setup_web_services.sh development --auto-start
./web_services/scripts/health_check.sh

# 서비스 시작/중지는 루트에서 가능
./start.sh
./stop.sh
```

**문제점**:
- ❌ 경로 불일치 (일부는 루트, 일부는 web_services/scripts/)
- ❌ 명령어 길이가 길어 타이핑 불편
- ❌ 사용자 혼란 (어디서 실행해야 하는지 모름)

### After (개선됨)
```bash
# 모든 명령을 프로젝트 루트에서 실행
./generate_env_files.sh development
./setup_web_services.sh development --auto-start
./health_check.sh
./start.sh
./stop.sh
```

**개선점**:
- ✅ 모든 명령이 프로젝트 루트에서 실행 가능
- ✅ 짧고 명확한 명령어 (setup_web_services.sh)
- ✅ 일관된 사용 경험
- ✅ 기존 스크립트는 그대로 유지 (하위 호환성)

---

## 📋 전체 웹 서비스 설치 (루트 실행)

```bash
# ============================================================
# Phase 0-2: 초기 설정 (프로젝트 루트)
# ============================================================
./collect_current_state.sh
./create_directory_structure.sh
pip3 install pyyaml jinja2

# ============================================================
# Phase 3: 환경 변수 생성 (프로젝트 루트)
# ============================================================
./generate_env_files.sh development

# ============================================================
# Phase 4: ONE-COMMAND 설치 (프로젝트 루트)
# ============================================================
./setup_web_services.sh development --auto-start

# ============================================================
# Phase 5: 확인 (프로젝트 루트)
# ============================================================
./health_check.sh
```

**모든 명령이 프로젝트 루트에서 실행됩니다!**

---

## 🎯 사용자 경험 개선

### 1. 일관성
모든 웹 서비스 관련 명령이 프로젝트 루트에서 실행되므로 사용자가 헷갈리지 않습니다.

### 2. 간결성
```bash
# Before
python3 web_services/scripts/generate_env_files.py development

# After
./generate_env_files.sh development
```

### 3. 발견 가능성
```bash
# 프로젝트 루트에서 ls 하면 바로 보임
ls *.sh

collect_current_state.sh
create_directory_structure.sh
generate_env_files.sh      # NEW!
health_check.sh             # NEW!
setup_cluster_full.sh
setup_web_services.sh       # NEW!
start.sh
stop.sh
```

---

## 🔍 래퍼 스크립트 구조

모든 래퍼 스크립트는 동일한 패턴을 따릅니다:

```bash
#!/bin/bash
################################################################################
# 스크립트 이름 래퍼 (프로젝트 루트 실행용)
################################################################################

# 프로젝트 루트로 이동
cd "$(dirname "$0")"

# 실제 스크립트 존재 확인
if [ ! -f "web_services/scripts/실제_스크립트.sh" ]; then
    echo "❌ 오류: 파일을 찾을 수 없습니다."
    exit 1
fi

# 모든 인자를 그대로 전달
./web_services/scripts/실제_스크립트.sh "$@"
```

**특징**:
- ✅ 현재 디렉토리에 영향받지 않음 (`cd "$(dirname "$0")"`)
- ✅ 파일 존재 여부 확인 (명확한 에러 메시지)
- ✅ 모든 인자를 실제 스크립트로 전달 (`"$@"`)
- ✅ 실행 권한 있음 (`chmod +x`)

---

## 📚 업데이트된 문서

다음 문서들이 새로운 루트 실행 방식을 반영하도록 업데이트되었습니다:

### 1. FULL_INSTALLATION_GUIDE.md
- ✅ 모든 명령어 경로 업데이트
- ✅ "모든 명령은 프로젝트 루트에서 실행" 명시

### 2. README.md
- ✅ "빠른 시작 (웹 서비스)" 섹션 업데이트
- ✅ 주요 명령어 섹션 업데이트

### 3. QUICKSTART_WEB.md
- ✅ 5분 빠른 시작 가이드 업데이트
- ✅ 모든 예제 명령어 업데이트

### 4. verify_automation.sh
- ✅ 루트 래퍼 스크립트 검증 추가
- ✅ 6개 새 체크 항목 추가 (33/33 통과)

---

## ✅ 검증 결과

```bash
./verify_automation.sh
```

**결과**: 33/33 통과 (100%)

**새로 추가된 검증 항목** (6개):
1. ✅ setup_web_services.sh 존재
2. ✅ setup_web_services.sh 실행 권한
3. ✅ generate_env_files.sh 존재
4. ✅ generate_env_files.sh 실행 권한
5. ✅ health_check.sh 존재
6. ✅ health_check.sh 실행 권한

---

## 💡 핵심 요약

### 생성된 파일 (5개)
```
프로젝트 루트/
├── start.sh                    (기존)
├── stop.sh                     (기존)
├── setup_web_services.sh       (NEW!)
├── generate_env_files.sh       (NEW!)
└── health_check.sh             (NEW!)
```

### 사용자 혜택
- ✅ **일관성**: 모든 명령이 루트에서 실행
- ✅ **간결성**: 경로가 짧고 명확
- ✅ **편의성**: 탭 완성으로 쉽게 찾기
- ✅ **호환성**: 기존 스크립트도 그대로 작동

### 명령어 예시
```bash
# Slurm 설치
./setup_cluster_full.sh

# 웹 서비스 설치
./generate_env_files.sh development
./setup_web_services.sh development --auto-start

# 서비스 관리
./start.sh
./stop.sh
./health_check.sh
```

**모든 것이 프로젝트 루트에서 실행됩니다!** 🚀

---

**작성일**: 2025-10-20
**버전**: 1.0
**결과**: 사용자가 별다른 신경쓸 필요 없이 **최상위 폴더에서 모든 명령 실행 가능**
